"use scrict";

const schedule = {
  2024: [
    [
      {
        numberDay: null,
        statusDay: "working", // 'weekend', 'holiday', '100%', '50%'
      },
    ],
  ],
};

function createYearSchedule(year) {}
